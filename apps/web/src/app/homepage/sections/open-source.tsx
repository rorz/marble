import { Section, SectionHeader } from "../ui/section";

export function OpenSourceSection() {
  return (
    <Section tone="darkest">
      <SectionHeader
        heading="Open source. By design."
        lede="Free to use forever. One-click install — do whatever you want, with the compromise of no support."
      />
      hello world
    </Section>
  );
}
