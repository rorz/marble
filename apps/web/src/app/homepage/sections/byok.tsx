import { Section, SectionHeader } from "../ui/section";

export function ByokSection() {
  return (
    <Section tone="mid">
      <SectionHeader
        eyebrow="Bring your own keys"
        heading="Or use ours — get started without any additional setup."
      />
    </Section>
  );
}
