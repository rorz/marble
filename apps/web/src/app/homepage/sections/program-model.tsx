import { Section, SectionHeader } from "../ui/section";

export function ProgramModelSection() {
  return (
    <Section tone="darkest">
      <SectionHeader heading="Every column is a program. Every program is shareable." />
    </Section>
  );
}
