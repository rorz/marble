{
  "author": "",
  "dependencies": {
    "@marble/contracts": "workspace:*",
    "@marble/lib": "workspace:",
    "@marble/sdk": "workspace:*",
    "@marble/store": "workspace:*",
    "hono": "catalog:base",
    "zod": "catalog:base"
  },
  "description": "A minimal example of using the Sandbox SDK",
  "devDependencies": {
    "@cloudflare/sandbox": "catalog:base",
    "@types/node": "catalog:base",
    "typescript": "catalog:base",
    "wrangler": "catalog:base"
  },
  "license": "MIT",
  "name": "@marble/executor",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --port 3087 --inspector-port 9231",
    "start": "wrangler dev --port 3087 --inspector-port 9231",
    "typecheck": "tsc --noEmit",
    "typegen": "wrangler types"
  },
  "type": "module",
  "version": "1.0.0"
}
